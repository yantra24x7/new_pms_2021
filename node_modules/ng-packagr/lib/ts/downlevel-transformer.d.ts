import { RawSourceMap } from 'source-map';
/**
 * Downlevels a .js file from `ES2015` to `ES5`. Internally, uses `tsc`.
 *
 */
export declare function downlevelEmitWithTsc(entryPoint: string, outDir: string): Promise<void>;
/**
 * Downlevels a .js file from `ES2015` to `ES5`. Internally, uses `tsc`.
 *
 * Required for some external as they contains `ES2015` syntax such as `const` and `let`
 */
export declare function downlevelCodeWithTsc(code: string, filePath: string): Promise<{
    code: string;
    map: RawSourceMap;
}>;
